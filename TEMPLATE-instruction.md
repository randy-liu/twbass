---
Title: 卷 {卷號}：{標題}
Volume_ID: {卷號}
Upstream_Required: {上游卷清單，格式：卷號 報告:{檔案名稱}（引用區塊）} | 無（本卷為最上游層）
Core_Parameters: {核心量化參數（含單位，逗號分隔）}
Key_Mechanisms: {核心機制名稱（2–5 個，逗號分隔）}
# ↓ 補充卷（SUP 系列）專用，一般卷刪除此行
# Research_Type: Supplemental
---

> ⚠️ **輸出格式強制要求（最高優先級）**
>
> 本次 Deep Research **必須輸出結構化報告，不可輸出純散文**。最終報告須包含以下區塊（缺任何一個均視為不完整）：
>
> 1. `Inherited_Baseline` — 列出引用的上游基準數值，標注來源編號
> 2. `{卷號}_Findings` — 8–15 條，每條以 `[V{卷號}-01]`、`[V{卷號}-02]`… 格式編號，每條必須含量化數值，**嚴禁模糊描述（高、低、強、弱）**
> 3. `Carry_Forward_To_{下游卷號}` — 可供下游卷直接引用的具體數值清單
> 4. `Unresolved_Dependencies` — 仍無法量化的缺口及建議後續研究方向
>
> **補充卷（SUP 系列）另須包含：**
> - `Correction_Instructions` — 對現有報告估算值的 `[確認]` 或 `[修正]` 建議
>
> 文末統一列參考文獻，**內文不使用 `[數字]` 引用標記**。

---

# 一、通用系統設定與輸出規範

1. **Metadata 區塊**：研究報告輸出時必須在最上方重現上述 Metadata 格式並填入實際研究結果。
2. **單位標準（全卷強制）**：溫度 °C、溶氧 mg/L、Eh mV、能見度 cm、時間 hr/day、氣壓 hPa、頻率 Hz{依本卷需要補充：、光強度 lux、波長 nm、聲速 m/s 等}。
3. **量化要求**：嚴禁使用「高、低、強、弱、顯著、明顯」等模糊描述；所有關鍵參數必須給出量化數值或合理估算區間。
4. **推測標示**：缺乏台灣直接實證時，必須明確標示為「基於〔引用理論/研究〕之推測」。
5. **反幻覺原則**：涉及黑鱸生理參數時，所有結論必須明確區分以下四個層級並標注來源：
   - **直接實驗證據**（*Micropterus salmoides* 直接量測）
   - **近緣物種外推**（鱸科 Centrarchidae，如 *M. dolomieu*、*Lepomis macrochirus*、*Perca flavescens*）——標注「類比推估」
   - **廣泛硬骨魚類外推**——標注「廣泛外推」
   - **純理論模型推算**——標注「理論估算」
6. **跨卷引用**：凡引用上游數值，必須標注來源編號（如 `B0-07`、`V1A-03`）；覆蓋 fallback 時標注「[覆蓋 fallback 假設]」。
   *(最上游層卷 0A / 0B 無上游依賴，刪除此點)*

---

# 二、計畫背景與本卷定位

本卷為《台灣大嘴黑鱸釣魚環境生態白皮書》**卷 {卷號}**，隸屬**{所屬冊名稱}**。

**本卷定位：** {說明本卷在整體架構中的角色；承接哪些上游輸出；產出什麼新知識層}

**本卷對下游的貢獻：**
- 卷 {下游卷1} 需引用本卷的 {具體輸出參數}
- 卷 {下游卷2} 需引用本卷的 {具體輸出參數}

本卷**不涉及** {排除範疇1}（那是卷 {負責卷號} 的任務）；**不涉及** {排除範疇2}（那是卷 {負責卷號} 的任務）。

---

# 三、系統設定

**你的角色：** 你現在是一位 {角色描述，2–3 個領域融合的 AI 專家}。請針對【卷 {卷號}：{標題}】進行 Gemini Deep Research。

**Gemini Deep Research 執行規則：**
1. 你會先根據本 prompt 生成 research plan；本文件所有條件視為**強制約束**，不得在 plan 階段自行擴張或跨出本卷 scope。
2. **若使用者已上傳 `{主要上游卷}`**，請優先引用其 `{引用區塊名稱}`（`{編號格式}`），標注引用編號。上傳的報告為權威性上游資料，優先於以下 fallback 數值。
3. {其他執行規則，如多份上游的引用優先順序}
4. 若你的預設 research plan 與本 prompt 衝突，以本 prompt 的 scope 規則為優先。
5. 在 research plan 中，請先明確列出：本卷研究邊界、要繼承的上游區塊、核心問題、來源類型、固定輸出區塊。

---

# 四、獨立執行與上游輸入規則

**優先情況（已上傳上游報告）：**
引用 `{上游區塊名稱}`（`{編號格式}`）中的 {具體參數類型}，標注引用編號。覆蓋 fallback 數值時標注「[覆蓋 fallback 假設]」。

**Fallback 情況（未上傳上游報告）：**
若未提供上游報告，先列出 `Missing_Upstream_Context`，然後使用以下 fallback 背景繼續分析（結論信心等級降為「中」）：

**六大水體精簡 fallback：**
1. 北部野生埤塘：水深 <3m；極育土黏重微酸性底質；能見度 30–50 cm；冬季具泥底熱慣性緩衝
2. 北部深水埤塘/水庫：水深 >10m；微酸性極育土促進絮凝；能見度 100–150 cm；暴雨易形成異重流
3. 北部管理池：水深 <2m；水車/增氧機非常態開啟（僅高溫低風時段啟動節電，平時關閉）；停機期 DO 依自然復氧；能見度 30–50 cm
4. 南部野生埤塘：水深 <2m；弱育土中等質地；旱季蒸發濃縮極端；春雨回淹 Fe³⁺ 還原風險
5. 南部深水埤塘/水庫：水深 >10m；弱育土；旱季消落帶礦化強；夏季暴雨產生巨大內部營養鹽負載
6. 南部管理池：水深 <2m；弱育土；曝氣機非常態開啟（僅高溫低風時段，平時關閉節電）；停機期夏季易轉硫酸鹽還原產生 H₂S

*(最上游層卷 0A / 0B 無上游依賴，刪除第四章全文，改為「本卷無上游文件需求，可完全獨立執行。」)*

---

# 五、台灣釣場情境與預設前提

*(補充卷 SUP 系列：將本章標題改為「五、上游基準數值（Inherited_Baseline 來源）」，列出已知基準數值供 Gemini 代入，格式見下方輸出區塊規格)*

{描述本卷特定的台灣情境背景；說明哪些水體類型、季節情境是本卷研究的核心場景；提供足以讓 Gemini 錨定台灣在地情境的前提假設}

---

# 六、核心研究清單

## Q1. {主要研究問題標題}

**Q1-1. {子問題}：**
- {具體研究要求，含量化精度需求}
- {引用的上游參數或 fallback 數值（若適用）}

**Q1-2. {子問題}：**
- {具體研究要求}

## Q2. {次要研究問題標題}

**Q2-1. {子問題}：**
- {具體研究要求}

*(依實際需求增加 Q3、Q4…)*

---

# 七、排除條件

以下主題**嚴格排除**：
- ❌ {排除項1}——那是卷 {負責卷號} 的任務
- ❌ {排除項2}——那是卷 {負責卷號} 的任務
- ❌ {排除項3}

---

# 八、最終輸出區塊規格

### `Inherited_Baseline`
列出實際引用的上游編號（`{編號格式}`）及對應量化數值。若為 fallback，標示「[Fallback]」及假設值。

### `{卷號}_Findings`
8–15 條，每條附唯一編號（格式 `V{卷號}-01`、`V{卷號}-02`…），每條為一個獨立量化發現。格式：
> [V{卷號}-XX]：[發現描述，含量化數值/估算區間]。[信心等級：高/中/低]。[若為外推，標示依據物種/理論]

*(SUP 卷：編號格式改為 `VSUP-{字母}NN`，如 `VSUP-A01`、`VSUP-DA01`)*

### `Carry_Forward_To_{下游卷號}`
列出本卷研究結果中可直接供卷 {下游卷號} 引用的具體數值。每條需明確指出：
(a) 參數類型、(b) 量化數值/估算區間、(c) 適用條件（水體類型/季節/Zone-A/B/C）、(d) 引用的 Findings 編號。

*(若有多個下游，分別建立多個 Carry_Forward 區塊)*

---

*(以下兩個區塊為補充卷 SUP 系列專用，一般主卷只保留 Unresolved_Dependencies)*

### `Correction_Instructions`
*(SUP 卷專用)* 針對上游報告現有估算值，列出「[確認]」或「[修正]」建議。格式：
```
目標：[上游報告名稱中的具體段落或發現編號]
現有數值：[XX]
建議更新為：[YY]（依據：[V{卷號}-XX]）
影響的 Findings/Rules：[引用編號列表]
```

---

### `Unresolved_Dependencies`
列出本卷研究後仍無法解決的缺口，說明：缺失什麼數據、影響哪些下游卷、建議的後續研究方向。
